{
  "name": "mobius-modules",
  "private": true,
  "description": "Mobius module frontends — one React+TS app per module, shared plumbing in shared/*. Backends live in mobius-api under /api/<slug>, gated by requireModule(slug).",
  "workspaces": [
    "shared/*",
    "_template",
    "countdown"
  ],
  "scripts": {
    "dev": "npm run dev -w @mobius-modules/template",
    "build": "npm run build --workspaces --if-present",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "typescript": "~5.9.3"
  }
}
