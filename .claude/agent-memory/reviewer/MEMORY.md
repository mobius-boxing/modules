# Memory Index

- [node-files hand-duplicated API types](node-files-duplicated-types.md) — modules/* frontends hand-copy mobius-api interfaces (no shared package); enum/shape fixes must be greped across type, DTO, AND every UI surface that lists the enum's values (dropdowns, filters), not just the type + label map.
